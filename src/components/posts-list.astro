---
import CardPost from "./card-post.astro";

const { posts } = Astro.props;
console.log(posts)
---

<main class="flex flex-col gap-2 w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 sm:py-6 lg:py-8 mt-10" >
    {posts.map((post) => (
       <CardPost post={post} />
    ))}
</main>
