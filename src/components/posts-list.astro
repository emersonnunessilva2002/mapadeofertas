---
import CardPost from "./card-post.astro";

const { posts } = Astro.props;
console.log(posts)
---

<article class="flex flex-col gap-2 w-[100vw] min-h-[100vh] p-10 desktop:flex-row">
    {posts.map((post) => (
       <CardPost post={post} />
    ))}
</article>
